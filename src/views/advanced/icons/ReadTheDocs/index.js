import React, { forwardRef } from "react"
import SvgReadthedocsioIcon from "./ReadthedocsioIcon"

export const ReadTheDocsIcon = forwardRef((props, ref) => {
  return <SvgReadthedocsioIcon ref={ref} {...props} />
})
